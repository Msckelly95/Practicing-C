#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "main.h"
/*
 * a program that prints all combinations of two digit numbers between 0-1
 */

int main()
{
	int x, y;

	for (x = 0; x <= 9; x++)

		for (y = x + 1; y <= 8; y++)
			_putchar(' ');
			_putchar(,);
	if (x != y)
		_putchar();
}
